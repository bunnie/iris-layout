# Layout Extraction Tools

`iris-layout` is a collection of scripts used to extract physical layout information
from design files (such as `lef`, `def`, `gdsii`) for the purpose of comparing against
IRIS imagery.
